import 'package:flutter/material.dart';

class MyColors {
  // Primary colors
  static const Color primary = Color(
    0xFF97CE4C,
  ); // Greenish color from the website
  static const Color secondary = Color(
    0xFF00A6A6,
  ); // Teal color from the website
  static const Color accent = Color(0xFFFFC107); // Yellow accent color

  // Background colors
  static const Color backgroundLight = Color(
    0xFFFFFFFF,
  ); // Light theme background
  static const Color backgroundDark = Color(
    0xFF121212,
  ); // Dark theme background
  static const Color surfaceLight = Color(0xFFF5F5F5); // Light surface color
  static const Color surfaceDark = Color(0xFF1E1E1E); // Dark surface color

  // Text colors
  static const Color textPrimaryLight = Color(
    0xFF000000,
  ); // Black text for light theme
  static const Color textPrimaryDark = Color(
    0xFFFFFFFF,
  ); // White text for dark theme
  static const Color textSecondaryLight = Color(
    0xFF757575,
  ); // Grey text for light theme
  static const Color textSecondaryDark = Color(
    0xFFB3B3B3,
  ); // Grey text for dark theme
}
